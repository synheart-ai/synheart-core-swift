import Foundation

/// BehaviorFeatureExtractor — gutted per RFC-CORE-0007.
///
/// All feature computation (tap rate, keystroke rate, scroll velocity,
/// idle ratio, burstiness, fragmentation, notification load, distraction
/// heuristic) has been removed. These are now computed by Flux.
class BehaviorFeatureExtractor {
    // Intentionally empty — all methods were RFC-CORE-0007 violations.
    // This file is retained as a placeholder; will be removed once
    // Flux FFI is fully wired.
}
